/* const nome = window.prompt('Qual seu nome?')

alert('É um grande prazer, '+ nome + '!') 
*/

let numero1 = Number(window.prompt('Digite um número')); // Preisa converter, pois window.prompt retorna string automaticamente.
let numero2 = Number(window.prompt('Digite outro número'));

let soma = numero1 + numero2;

alert(`A soma entre ${numero1} e ${numero2} é igual a ${soma}`); // Precisa ser em crase


/* 

soma.lenght = Quantos caracteres tem
soma.toUpperCase() = Coloca para letras maiúsculas
soma.toLowerCase() = Coloca para letras minúsculas

*/